# Managed by gitops-forge (governance)
# Hash: 77b97343
# Do not edit manually — this file is regenerated by gitops-forge apply
name: Label Validator
on:
  issues:
    types: [opened, labeled, unlabeled]
  pull_request:
    types: [opened, labeled, unlabeled]
concurrency:
  group: label-validator-${{ github.event.issue.number || github.event.pull_request.number || github.run_id }}
  cancel-in-progress: true
jobs:
  validate:
    uses: Jurel89/gitops-forge/.github/workflows/label-validator.yml@main
